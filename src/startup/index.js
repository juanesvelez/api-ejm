const express = require('express');

let _express = null;
let config = null

class Server{
    constructor({config , router}){
        _config = config;
        _express = router

    }


    
}

module.exports = Server;